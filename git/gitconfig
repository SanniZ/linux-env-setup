[user]
	email = sanni230@126.com
	name = Byng Zeng

[includeIf "gitdir:~/.mygit/"]
    path = .gitconfig-self
[includeIf "gitdir:~/workspace/"]
    path = .gitconfig-work
	
[alias]
    amend = commit --amend
    amendf = commit --amend --no-edit
    br = branch
    bra = branch -a
    brb = brnach -b
    brd = branch -d
    brD = brnach -D
    ct = commit
    co = checkout
    cp = cherry-pick
    df = diff
    dt = difftool
    ds = diff -- staged
    l = log
    lg = log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(bold white)— %an%C(reset)%C(bold yellow)%d%C(reset)' --abbrev-commit --date=relative
    lga = log --author
    lp = log --pretty=oneline
    sa = stash apply
    sh = show
    ss = stash save
    st = status
    
[color]
    ui = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[color "diff"]
    meta = yellow
    frag = magenta bold
    commit = yellow bold
    old = red bold
    new = green bold
    whitespace = red reverse

[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = red bold 52
    newNormal = green bold
    newHighlight = green bold 22
[filesystem "Private Build|1.8.0_222|/dev/sda1"]
	timestampResolution = 1001 microseconds
	minRacyThreshold = 6585 microseconds
